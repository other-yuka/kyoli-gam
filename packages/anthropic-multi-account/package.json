{
  "name": "opencode-anthropic-multi-account",
  "version": "0.2.22",
  "description": "OpenCode plugin for Anthropic multi-account management with automatic rate limit switching",
  "main": "./dist/index.js",
  "type": "module",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "for f in tests/*.test.ts; do bun test \"$f\" || exit 1; done",
    "check:fingerprint-drift": "bun run build && node scripts/check-fingerprint-drift.mjs",
    "check:cc-authorize-probe": "bun run build && node scripts/check-cc-authorize-probe.mjs",
    "bake:fingerprint": "bun run build && ALLOW_FINGERPRINT_OVERWRITE=1 node scripts/capture-and-bake.mjs",
    "prepack": "bun run build",
    "dev": "bun run build --watch"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "sideEffects": false,
  "keywords": [
    "opencode",
    "plugin",
    "anthropic",
    "claude",
    "multi-account",
    "rate-limit"
  ],
  "author": "yuka",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/other-yuka/kyoli-gam.git",
    "directory": "packages/anthropic-multi-account"
  },
  "dependencies": {
    "opencode-multi-account-core": "^0.2.22",
    "valibot": "^1.2.0"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^0.15.30",
    "@types/node": "^25.2.2",
    "typescript": "^5.8.0"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public"
  },
  "files": [
    "dist",
    "package.json",
    "README.md",
    "LICENSE"
  ]
}
