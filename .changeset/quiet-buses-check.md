---
"opencode-anthropic-multi-account": patch
---

Add the headless Claude Code authorize probe, wire the drift-watch fallback flow, and share the probe contract across fetch and browser implementations.
