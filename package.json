{
  "name": "kyoli-gam-monorepo",
  "private": true,
  "packageManager": "bun@1.3.8",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "esbuild": "^0.25.0",
    "tsx": "^4.19.0",
    "turbo": "^2.5.0"
  }
}
